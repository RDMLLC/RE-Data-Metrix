Fix the ARV Result section at the bottom of ArvHelperOverlay.tsx. Read the file in full before making changes.

The current layout is cramped and broken — the formula text and buttons are on the same line, formula is wrapping, everything is squeezed.

Rebuild the ARV Result section as a clean sticky footer bar at the bottom of the overlay (fixed, not scrolling), structured exactly like this:

┌─────────────────────────────────────────┐
│ Suggested ARV                           │  ← text-xs text-muted-foreground
│ $255,840                                │  ← text-2xl font-bold text-primary
│ $120/sqft × 2,132 sqft — 4 comps       │  ← text-xs text-muted-foreground, whitespace-nowrap, truncate
├─────────────────────────────────────────│
│ [Download Comp Report] [Use This ARV]   │  ← two buttons, flex row, full width
└─────────────────────────────────────────┘

Implementation:
- Remove whatever current ARV result rendering exists (sticky footer, inline card, or whatever is there now)
- Replace with a fixed bottom bar: className="fixed bottom-0 left-0 right-0 bg-background border-t border-border px-4 pt-3 pb-4 z-[10002]"
- Inside: 
  Top section (the three text lines):
    <p className="text-xs text-muted-foreground">Suggested ARV</p>
    <p className="text-2xl font-bold text-primary">{formatCurrency(calculatedArv)}</p>
    <p className="text-xs text-muted-foreground whitespace-nowrap truncate">
      {formatCurrency(avgPricePerSqft)}/sqft × {effectiveSqft.toLocaleString()} sqft — {selectedCount} comps
    </p>
  Divider: <div className="border-t border-border my-2" />
  Button row:
    <div className="flex gap-2">
      <Button variant="outline" className="flex-1 text-sm" onClick={handleExportPdf} disabled={selectedCount < 2}>
        Download Comp Report
      </Button>
      <Button className="flex-1 text-sm" onClick={handleApplyArv} disabled={calculatedArv <= 0}>
        Use This ARV
      </Button>
    </div>

- Add padding to the bottom of the scrollable content area so comps are not hidden behind the fixed footer:
  Add pb-40 (or pb-36) to the scrollable main container so the last comp card is fully visible above the footer

- The fixed footer should only be visible after the first search returns results (same condition as before — hide when no compsData)

- Remove any previous sticky/fixed footer implementation to avoid duplicates

- Distance field fix: check ArvHelper.tsx for the exact field name on comp objects for distance (distanceMiles, distance, distanceFromSubject etc) and make sure the comp cards show it correctly formatted as "X.X mi". If the value is 0 or missing show "—"

After fixes confirm dev server reloads cleanly. Output the ARV Result footer section and the scrollable container className with line numbers.